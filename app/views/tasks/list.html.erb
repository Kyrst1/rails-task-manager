<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%=link_to task.title, task_path(task) %>
    <%=link_to "destroy", destroy_path(task),data: { confirm: "Are you sure?" }, method: :delete%></li>
            
  <% end %>
</ul>

<h5><%=link_to "Add a task", new_path %></h5>
<h4></h4>